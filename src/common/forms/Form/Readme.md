### Form
```jsx
const Button = require('../../buttons/Button').default;

    <Form onSubmit={ () => console.log('submitted') }> 
        <Button type="submit">
            Submit
        </Button>
    </Form>
```
