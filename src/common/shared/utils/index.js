import { curryWithPressed, curryWithPreventDefault, curryWithEnterKeyPress } from './events'
import hotKeys from './hotKeys'

export { curryWithPressed, curryWithPreventDefault, curryWithEnterKeyPress, hotKeys }

hotKeys()
